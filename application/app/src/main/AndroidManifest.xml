<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="surviveanpylace.com.surviveanyplace">

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon_knowledge"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SelectLanguage" />
        <activity
            android:name=".scrolly"
            android:label="@string/title_activity_scrolly"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".SelectTier"></activity>
    </application>

</manifest>